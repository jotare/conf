# -*- mode: shell-script -*-

alias spacemacs='SPACEMACS=1 emacs'
alias texclean='rm -f *.toc *.aux *.log *.cp *.fn *.tp *.vr *.pg *.ky'
alias magic='magic -noconsole -T scmos'
alias octave='octave --no-gui' # can also use octave-cli
